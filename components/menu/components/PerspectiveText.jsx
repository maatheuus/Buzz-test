function PerspectiveText({ label }) {
  return (
    <div className="perspectiveText text-black flex flex-col justify-center items-center h-full w-full">
      <p>{label}</p>
      <p>{label}</p>
    </div>
  );
}

export default PerspectiveText;
